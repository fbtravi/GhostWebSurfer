{
  "name": "ghost-web-surfer",
  "version": "1.1.0",
  "description": "A script to simulate multiple concurrent users accessing a URL using Playwright.",
  "main": "simular-usuarios.js",
  "scripts": {
    "start": "node simular-usuarios.js",
    "dashboard": "OUTPUT_MODE=dashboard node simular-usuarios.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npx playwright install --with-deps"
  },
  "keywords": [
    "playwright",
    "load-testing",
    "automation",
    "simulator"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "blessed": "^0.1.81",
    "blessed-contrib": "^4.11.0",
    "cli-progress": "^3.12.0",
    "p-limit": "^4.0.0",
    "playwright": "^1.44.0"
  }
}
